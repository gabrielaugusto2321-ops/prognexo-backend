import{describe,it,expect,vi,beforeEach}from'vitest';
const state={row:null,doctorIds:[]};const chain={select(){return this},eq(){return this},maybeSingle:async()=>({data:state.row})};vi.mock('../src/lib/supabase.js',()=>({supabase:{from:vi.fn(()=>chain)}}));vi.mock('../src/middleware/auth.js',()=>({getScopedDoctorIds:vi.fn(async()=>state.doctorIds)}));const{authorizeResource}=await import('../src/lib/authz.js');
describe('tenancy authorization',()=>{beforeEach(()=>{state.row={id:'lead-b',doctor_id:'doctor-b',sdr_responsavel_id:'closer-b'};state.doctorIds=['doctor-a'];});it('denies cross-clinic resources',async()=>expect((await authorizeResource({user:{id:'doctor-a-user',role:'doctor'},table:'leads',id:'lead-b'})).reason).toBe('forbidden'));it('denies a closer another closer portfolio',async()=>{state.doctorIds=['doctor-b'];expect((await authorizeResource({user:{id:'closer-a',role:'closer'},table:'leads',id:'lead-b',requireOwnerForCloser:true})).ok).toBe(false);});});
