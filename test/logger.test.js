import{describe,it,expect}from'vitest';import{Writable}from'node:stream';import{createLogger}from'../src/lib/logger.js';
describe('logger redaction',()=>{it('redacts secrets and personal data',async()=>{let output='';const sink=new Writable({write(c,_e,cb){output+=c.toString();cb();}});const log=createLogger(sink);log.info({req:{headers:{authorization:'Bearer secret'},query:{secret:'query-secret'}},password:'Password123',cartao:{numero:'4111111111111111',ccv:'123'},telefone:'5511999999999',conteudo:'private'},'test');await new Promise(r=>setImmediate(r));for(const secret of ['Bearer secret','query-secret','Password123','4111111111111111','123','5511999999999','private'])expect(output).not.toContain(secret);expect(output).toContain('[REDACTED]');});});
